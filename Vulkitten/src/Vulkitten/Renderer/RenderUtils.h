#pragma once

#include "Vulkitten/Core/Core.h"

namespace Vulkitten {

// ============================================================
// RenderUtils — placeholder for rendering utility functions.
// Will hold helper functions for common render operations
// (fullscreen quad, mipmap generation, cubemap conversion, etc.).
// Empty stub for now — methods added in later tasks.
// ============================================================
class VKT_API RenderUtils
{
public:
    RenderUtils() = default;
};

} // namespace Vulkitten
