1. 创建 PerFrameSceneData 结构体到 RenderGraph：包含 Camera*、glm::mat4 ViewProjection、LightData 空壳占位。添加 `SetPerFrameData(const PerFrameSceneData&)` / `GetPerFrameData() → const PerFrameSceneData*` 方法。更新 RenderSystem::OnUpdate 在每帧开始时收集场景主相机和 VP 矩阵，通过 `RenderContext::Get().GetRenderGraph()->SetPerFrameData(data)` 写入。更新 SpriteRenderPass、PreparePass、GpuParticlePass 的 onExecute 从 `graph->GetPerFrameData()` 获取相机/VP，移除原来各自调用 `graph->GetSceneCamera()` / `graph->GetViewProjection()` 的分散查询。移除 RenderGraph 上 `SetSceneCamera`/`GetSceneCamera`/`SetViewProjection` 方法
2. 将 FrameBuffer 从 RenderGraph 运行时查询移到 RenderPass 配置：在 RenderPass 添加 `SetTargetFramebuffer(Ref<Framebuffer>)` 和 `GetTargetFramebuffer()`。RenderGraph::Execute() 遍历 Pass 时，若 Pass 配置了 Framebuffer 则在 onExecute 前 Bind、后 Unbind。移除 PreparePass 和 SpriteRenderPass onExecute 中 `graph->GetFramebuffer()` 查询和手动 Bind/Unbind。移除 RenderGraph 上 `SetFramebuffer`/`GetFramebuffer` 方法。用户（EditorLayer）改为 `pass.SetTargetFramebuffer(fb)` 方式配置
3. 消除 PreparePass 和 Renderer::OnWindowResize 对 Legacy::RenderCommand 的依赖：在 Renderer 中添加 `RendererAPI*` 成员（构造时从 `RendererAPI::Create()` 创建）。PreparePass::onExecute 中 `Legacy::RenderCommand::SetClearColor` → `m_RendererAPI->SetClearColor`，`Legacy::RenderCommand::Clear` → `m_RendererAPI->Clear`。Renderer::OnWindowResize 中 `Legacy::RenderCommand::SetViewport` → `m_RendererAPI->SetViewport`。Renderer::Shutdown 负责销毁 RendererAPI 实例。确保 Renderer::Init 不再调用 `Legacy::RenderCommand::Init()`
4. 将 Renderer2D 批处理渲染逻辑内联到 SpriteRenderPass：在 SpriteRenderPass 中创建 QuadVertexArray(VAO+VBO+IBO)、TextureShader、WhiteTexture 作为成员（从 Renderer2DData 迁移）。将 BatchVertex 结构体、SortAndFlush/Flush/DrawQuad 方法从 Renderer2D 复制到 SpriteRenderPass 作为私有方法。修改 onExecute 中的 `Renderer2D::DrawQuad` 调用为内部方法。确保 SpriteRenderPass 不再 `#include "Renderer2D.h"`
5. 移除 Renderer2D 类：删除 `Vulkitten/Renderer/Renderer2D.h/.cpp`（约 430 行）。从 `Renderer::Init/Shutdown` 中移除 `Renderer2D::Init()` / `Renderer2D::Shutdown()` 调用。从 `Vulkitten.h` 统一头文件中移除 `#include "Renderer2D.h"`。移除 Sandbox 中 ImGui 调试 UI 对 `Renderer2D::GetStats()` 的引用（Sandbox2D::OnImguiRender 中的 Renderer Stats 面板）
6. 移除 Legacy::RenderCommand 类：删除 `Vulkitten/Renderer/RenderCommand.h`（Legacy 命名空间）。确认所有 Legacy::RenderCommand 的调用点已被 Task 3 替换完毕。确认 `Renderer::Init` 已不再调用 `Legacy::RenderCommand::Init()`。确保 `RenderCommand.cpp`（若存在）一并移除
7. 修复 Sandbox ExampleLayer2 的窗口 Resize 画面变形：在 ExampleLayer2 的 OnEvent 中处理 `WindowResizeEvent`——获取新的窗口宽高，计算新 aspect ratio，更新场景中主相机的 `SceneCamera::SetOrthographicProjection()` 参数。确保 OnUpdate 中 Scene::OnUpdate 使用更新后的相机投影矩阵。验证 Resize 后画面不再拉伸变形
