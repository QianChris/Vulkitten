1. 创建平台层抽象接口IWindow/ISurface/SurfaceDesc：在Vulkitten/Core/新建IWindow.h定义GetSurfaceDesc()和CreateSurface()纯虚方法返回ISurface；新建ISurface.h定义平台绘制表面抽象+SurfaceDesc结构体（Width/Height/Format等）；WindowsWindow在现有Window继承外额外实现IWindow接口；创建WindowsSurface（GL下封装GLFW窗口）实现ISurface
2. 创建后端接口IRenderer和RendererConfig配置结构体：在Vulkitten/Renderer/新建IRenderer.h，定义生命周期纯虚方法BeginFrame/EndFrame/Execute/OnWindowResize以及GetDevice/GetResourceManager/GetRenderGraph访问器；RendererConfig结构体包含IDevice*/RenderGraph*/IGpuResourceManager*三个必须依赖
3. 将现有Device重命名为IDevice并更新OpenGLDevice/Application/Renderer等全部引用；将GpuResourceManager抽象为IGpuResourceManager接口（定义CreateTexture/CreateBuffer/CreateShader/CreatePipeline/CreateGeometry纯虚方法返回安全句柄），现有GpuResourceManager继承IGpuResourceManager实现接口；后续Pass和外部代码不再允许直接new GPU资源
4. 创建FrameContext结构体承载每帧状态：在Vulkitten/Renderer/新建FrameContext.h，定义FrameIndex/SwapchainIndex/CommandPool(OpenGL为空壳)/CommandBuffer/Fence(OpenGL为空壳)/Semaphore(OpenGL为空壳)/PerFrameResourcePool字段，接口层面为Vulkan后端预留完整结构
5. 将现有RenderContext重命名为RendererSubsystem并更新Vulkitten.h/Application.cpp/Scene.cpp/RenderSystem.cpp/ClassFactory.cpp/SpriteRenderPass.cpp/PreparePass.cpp/GpuParticlePass.cpp/GpuParticle.h/Buffer.cpp/Texture.cpp/GraphicContext.cpp等全部引用点；创建新的Per-Pass RenderContext（Vulkitten/Renderer/RenderContext.h），持有FrameContext&和IRenderer&，提供TranslateCommand(RenderCommand)方法将Command翻译为API Drawcall，维护临时PipelineHandle/GeometryHandle映射表
6. 让现有Renderer实现IRenderer接口：Renderer继承IRenderer，BeginFrame()中创建当前帧FrameContext+CommandPool；EndFrame()执行Fence同步+SwapchainPresent（EndPass的SwapBuffers逻辑迁移到EndFrame）；Pass执行回调改为接收Per-Pass RenderContext替代直接调用RendererAPI；SpriteRenderPass/PreparePass的onExecute改用RenderContext翻译命令
7. 创建SceneContext结构体并重构Scene::OnUpdate接受参数注入：在Vulkitten/Scene/新建SceneContext.h，包含IRenderer&和RenderGraph&引用；Scene::OnUpdate(Timestep ts, SceneContext& ctx)替代内部直接调用单例；RenderSystem从SceneContext获取RenderGraph写入RenderCommand；更新所有调用Scene::OnUpdate处传入SceneContext
8. 更新Application::Run主循环使用IRenderer生命周期（BeginFrame→Layer::OnUpdate(ts, sceneCtx)→ImGui绘制→EndFrame→Execute），迁移ExampleLayer2::OnUpdate(Timestep, SceneContext&)通过注入获取RenderGraph，验证Sandbox中3个Quad渲染正常+窗口Resize正常，清理旧代码路径
9. 文件夹结构迁移：将src/Platform/OpenGL/下全部18个文件（OpenGLBuffer/OpenGLContext/OpenGLDevice/OpenGLFramebuffer/OpenGLRendererAPI/OpenGLShader/OpenGLTexture/OpenGLUtil/OpenGLVertexArray .h/.cpp）移至src/Vulkitten/Renderer/Backend/OpenGL/；将src/Platform/Windows/下全部5个文件（WindowsWindow/WindowsInput .h/.cpp + WindowsFileDialogs.cpp）移至src/Vulkitten/Window/Platform/Windows/；更新Buffer.cpp/Framebuffer.cpp/VertexArray.cpp/Texture.cpp/Shader.cpp/Renderer.cpp/RenderCommand.cpp中"Platform/OpenGL/..."引用为"Vulkitten/Renderer/Backend/OpenGL/..."；更新Application.cpp引用为"Vulkitten/Renderer/Backend/OpenGL/OpenGLDevice.h"；更新WindowsWindow.h/.cpp交叉引用为"Vulkitten/Window/Platform/Windows/..."；CMake的GLOB_RECURSE自动适配新目录无需修改
10. 集成Vulkan SDK到CMake构建系统并在Vulkitten/Renderer/Backend/Vulkan/创建VulkanInstance封装：在CMakeLists.txt中添加find_package(Vulkan REQUIRED)和target_link_libraries(Vulkitten PUBLIC Vulkan::Vulkan)；创建VulkanInstance.h/.cpp封装vkCreateInstance→VkInstance（含VK_EXT_debug_utils扩展的Debug Messenger），支持Validation Layer可配置开启，提供GetVkInstance()访问器
11. 创建VkDevice实现IDevice接口并注册到ClassFactory：在Vulkitten/Renderer/Backend/Vulkan/创建VkDevice.h/.cpp，继承IDevice实现Init(物理设备枚举：按VK_PHYSICAL_DEVICE_TYPE_DISCRETE_GPU优先级选择，查询Queue Family获取Graphics+Present+Transfer队列索引)/Shutdown(vkDestroyDevice)；通过ClassFactory::RegisterInterface<IDevice, VkDevice>()注册
12. 实现VkSurface和VkSwapchain管理类：在Vulkitten/Renderer/Backend/Vulkan/创建VkSwapchain.h/.cpp，VkSurface从IWindow::CreateSurface()获取（GLFW→glfwCreateWindowSurface→VkSurfaceKHR）；VkSwapchain负责vkCreateSwapchainKHR/AcquireNextImageKHR(vkAcquireNextImageKHR+ImageAvailableSemaphore)/Present(vkQueuePresentKHR+RenderFinishedSemaphore)；含VkImageView和VkFramebuffer的Per-Image数据管理；支持窗口Resize时Swapchain重建
13. 创建VulkanFrameContext和VulkanRenderContext实现Per-Frame和Per-Pass命令翻译：VkFrameContext继承FrameContext含VkCommandPool/VkFence(SubmitInFlight)/VkSemaphore(ImageAvailable+RenderFinished)/VkCommandBuffer[MAX_FRAMES_IN_FLIGHT]数组；VkRenderContext继承RenderContext实现TranslateCommand将DrawQuadCommand翻译为vkCmdBindVertexBuffers+vkCmdBindIndexBuffer+vkCmdDrawIndexed，ClearCommand翻译为vkCmdClearAttachments，负责VkPipeline/VkDescriptorSet的绑定和PushConstant更新
14. 创建VkGpuResourceManager实现IGpuResourceManager的Buffer和Texture资源管理：在Vulkitten/Renderer/Backend/Vulkan/创建VkGpuResourceManager.h/.cpp，继承IGpuResourceManager实现CreateBuffer(VkBuffer+VkDeviceMemory+staging)/CreateTexture(VkImage+VkImageView+VkSampler+VkDeviceMemory，含staging buffer上传和image layout transition)/DestroyResource；可选集成VulkanMemoryAllocator(VMA)简化内存分配；所有资源通过句柄(index+generation)返回
15. 实现VkShader编译(GLSL→SPIR-V)和VkPipeline创建：在Vulkitten/Renderer/Backend/Vulkan/创建VkShader.h/.cpp实现运行时GLSL→SPIR-V编译(VulkanSDK自带glslang或shaderc)→VkShaderModule；创建VkPipeline.h/.cpp实现CreatePipeline(CreateGraphicsPipeline含VkPipelineLayout→VkDescriptorSetLayout→ShaderStages→VertexInputState(SpriteRenderPass的BatchVertex布局)→VkPipeline)，支持VkRenderPass兼容，返回PipelineHandle
16. 创建VkRenderer实现IRenderer接口串联全部Vulkan组件：在Vulkitten/Renderer/Backend/Vulkan/创建VkRenderer.h/.cpp，继承IRenderer实现BeginFrame(AcquireNextImage+创建VkFrameContext)→Execute(遍历RenderGraph Pass,每Pass获取VkCommandBuffer→创建VkRenderContext记录命令→vkEndCommandBuffer)→EndFrame(vkQueueSubmit+Fence同步→Present→垃圾回收当前帧资源)；整合VkSwapchain重建(窗口Resize时重建Swapchain+Framebuffer)
17. 端到端集成Vulkan后端渲染路径并验证：在RendererSubsystem/RendererConfig中添加Backend枚举(OpenGL/Vulkan)和选择逻辑；Application构造函数根据RendererConfig.backend分支创建OpenGLDevice/VkDevice和对应IRenderer实现；验证Sandbox中ExampleLayer2用Vulkan后端渲染3个Quad(FlatColor+Texture)正常+窗口Resize正常(含Swapchain自动重建)+ImGui Overlay正常显示；清理OpenGL硬编码路径确保后端可切换
