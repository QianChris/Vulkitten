1. 删除 GraphicContext.h/.cpp 和 RendererSubsystem.h/.cpp：GraphicContext 已无外部调用者（仅自引用）；移除 Shader.h 中 `friend class RendererSubsystem`；Buffer.cpp 的误引用 `#include "RendererSubsystem.h"` 改为 `#include "Vulkitten/Renderer/IRenderer.h"`；Vulkitten.h umbrella 移除相应 include
2. 将 GpuResourceManager.h/.cpp 移至 Backend/OpenGL/ 并重命名为 OpenGLGpuResourceManager：GpuTextureDesc/GpuBufferDesc/GpuResourceSlot 结构体移至 IGpuResourceManager.h（作为平台无关资源描述符）；更新所有 include 引用（Renderer.cpp/Buffer.cpp/Texture.cpp/VkGpuResourceManager.h/Application.cpp 等）
3. 将 Renderer 重命名为 OpenGLRenderer 并移至 Backend/OpenGL/：Renderer.h 保留为平台无关基类（含 IDevice/IGpuResourceManager/RenderGraph/ShaderLibrary 成员）；OpenGLRenderer 继承 Renderer，在 Init() 中创建 OpenGLDevice+OpenGLGpuResourceManager+OpenGLRendererAPI+Pass 注册；Application 中改为 `CreateScope<OpenGLRenderer>(config)` 或 `CreateScope<VkRenderer>(config)`
4. 清理 Pass 目录中平台相关引用：SpriteRenderPass/PreparePass/GpuParticlePass 中将 `static_cast<Renderer&>(IRenderer::Get()).GetRendererAPI()` 的直接 RendererAPI 调用改为通过 IRenderer 提供的虚接口；在 IRenderer 中添加 `virtual void DrawIndexed(...)` 等绘图虚方法，由各后端实现在 RenderContext 翻译中调用
5. 移除 Shader::Create/Buffer::Create/Texture2D::Create 静态工厂方法：所有 GPU 资源创建统一走 IGpuResourceManager.CreateShader(desc)→handle / CreateTexture(desc)→handle / CreateBuffer(desc)→handle，返回 uint64_t 安全句柄；Shader/Buffer/Texture 类保留 Bind/Unbind/SetUniform 等使用方法但不再有 Create 静态方法；SpriteRenderPass 和 Renderer::Init() 中 shader 创建改为通过 IGpuResourceManager
6. 重构 FrameContext 为每帧 transient 实例：移除 Renderer 中的 `Scope<FrameContext> m_FrameContext` 成员；BeginFrame() 返回/创建 FrameContext（含 swapchain image index 和同步原语）→ 传入 RenderGraph::Execute(FrameContext&) 进行命令录制 → EndFrame() 调用 IDevice::Submit(FrameContext&) 完成命令提交+同步+Present；在 IDevice 接口中新增 `virtual void Submit(FrameContext& frameContext) = 0` 纯虚方法，OpenGLDevice 实现为 SwapBuffers（或通过 backendContext），VulkanDevice 实现为 vkQueueSubmit+Present
7. 端到端验证：Sandbox 使用 OpenGL 后端确认 3 Quad 渲染正常 + 窗口 Resize 正常 + 所有资源通过 IGpuResourceManager 句柄创建；Vulkan 后端编译通过（白屏已知，Task 8 实现最小渲染）
8. 实现 Vulkan 后端最小清屏渲染：VkSwapchain 真实创建 VkSurfaceKHR+VkSwapchainKHR（含 surface format 选择）；VkRenderer::Init() 创建 VkRenderPass+VkFramebuffer（Per-Image）；BeginFrame 中 vkAcquireNextImageKHR+vkBeginCommandBuffer+vkCmdBeginRenderPass+vkCmdClearAttachments+vkCmdEndRenderPass+vkEndCommandBuffer；EndFrame 中 IDevice::Submit（vkQueueSubmit+Fence+Present）；验证 RenderDoc 可抓帧+显示清屏颜色
