1. 增强 ClassFactory：添加 `CreateInstance<T>()`(内部 Meyer's lazy singleton)、`RegisterInstance<T>(T*)`(注册外部创建的单例)、`GetInstance<T>()`(获取已注册单例)、`RegisterInterface<I, Impl>()`(注册接口实现映射)、`GetInterface<I>()`(获取接口实现引用) —— 引擎 DI 基础，所有后续单例通过 ClassFactory 统一管理。不包含插件注册（后续做）
2. 创建 Device 抽象类 (Vulkitten/src/Vulkitten/Renderer/Device.h)，定义纯虚接口 Init/Shutdown 等基础 GPU 操作。创建 OpenGLDevice 空壳实现 (Platform/OpenGL/OpenGLDevice.h/.cpp) 作为占位，内部委托给现有 RendererAPI。留出 Vulkan 扩展空间
3. 创建 Engine 单例类 (Vulkitten/src/Vulkitten/Core/Engine.h/.cpp)，通过 ClassFactory::GetInstance<Engine>() 获取。Engine 持有：FileSystem(实例)、Input(管理平台 Input 的创建/销毁生命周期，保持 Input::IsKeyPressed() 静态外观)、Log(GetLogger() 便捷方法返回引用，VKT_CORE_* 宏不变)、EventQueue(空壳 struct)、ThreadPool(空壳 struct)。Engine::Init() 进行初始化，Engine::Shutdown() 清理资源
4. FileSystem 实例化——仅改 FileSystem 自身：RegisterPath/Resolve/Exists 改为实例方法，构造函数为空。保留一套静态包装方法 `FileSystem::Resolve(path)` 内部转发到 `Engine::Get().GetFileSystem().Resolve(path)` 作为过渡。此步只修改 FileSystem.h/.cpp，项目其余部分编译不受影响
5. FileSystem 调用点迁移：将所有 `FileSystem::RegisterPath()` / `FileSystem::Resolve()` 调用点改为 `Engine::Get().GetFileSystem().RegisterPath()` / `Resolve()`。涉及 Vulkitten 内部（Application.cpp、OpenGLShader.cpp、Texture.cpp、SceneSerializer.cpp 等）和 Sandbox（SandboxApp.cpp、ExampleLayer2.h）。迁移完成后移除任务 4 中添加的静态过渡包装
6. 创建 GpuResourceManager (Vulkitten/src/Vulkitten/Renderer/GpuResourceManager.h/.cpp)。统一管理显存资源：`uint64_t CreateTexture(desc)` / `CreateBuffer(desc)` 返回句柄，`GetTexture(handle)` / `GetBuffer(handle)` 通过句柄查找。内部使用 map<uint64_t, PoolSlot>，句柄编码 generation 防悬空（复用 RenderGraph ResourcePool 的 Handle 设计）。支持延时创建（记录 desc，首次 Get 时才分配 GPU 资源）。暂不做现有 Texture/Buffer 类迁移，仅创建 Manager 骨架 + 句柄分配/查找逻辑
7. 创建 ShaderManager (Vulkitten/src/Vulkitten/Renderer/ShaderManager.h/.cpp)。构造函数注入 FileSystem&（来自 Engine::Get().GetFileSystem()）。提供 `uint64_t LoadShader(virtualPath)` 解析 #include 后编译，返回 shader 句柄。内部委托 GpuResourceManager 管理句柄。将 OpenGLShader 的 #include 预处理逻辑提取到 ShaderManager 中。暂不替换现有 Shader 类的 Load 路径，仅新增 ShaderManager 并验证单文件加载
8. 创建 RenderContext 单例 + 重构 Renderer 为实例：创建 RenderContext (Vulkitten/src/Vulkitten/Renderer/RenderContext.h/.cpp)，通过 ClassFactory 创建并指定后端。RenderContext 持有：Renderer(实例，替代当前静态 Renderer)、RenderGraph(从 Renderer 移入)、RenderUtils(空壳)。Renderer 持有：Device*(从 RenderContext 拿到的后端 Device)、GpuResourceManager&、ShaderManager&。重构 Renderer.h/.cpp：Init/Render/Shutdown 从静态方法变为实例方法；RenderGraph 生命周期从 Renderer 移到 RenderContext；BeginScene/EndScene/Submit 暂时移除（未使用）。更新 Application.cpp 的 Renderer::Init/Render 调用点
9. 创建 GraphicContext 单例 (Vulkitten/src/Vulkitten/Core/GraphicContext.h/.cpp)，通过 ClassFactory 创建，构造函数接收 RenderContext&。GraphicContext 持有：Window(通过 Window::Create 创建，当前仅支持 Windows GLFW)、SceneUtil(空壳)、GraphicUtil(空壳)。Window 的 SwapBuffers 仍通过 EndPass 调用（从 RenderContext 的 RenderGraph 拿 backendContext），GraphicContext 不介入渲染流程
10. Engine 添加 Scene 工厂方法：在 Engine.h/.cpp 中添加 `CreateEmptyScene()` 返回 Scope<Scene>（含空白 entt::registry）、`LoadSceneFromGltf(filepath)` 空壳接口（预留，返回 nullptr 或空 Scene）、`MergeScenes(Scene& target, Scene& source)` 遍历 source.GetRegistry() 克隆所有 entity 和 Component 到 target
11. Scene 添加 System 执行顺序配置：在 Scene.h/.cpp 中添加 `SetSystemOrder(vector<string> systemNames)` 指定执行顺序。Scene::OnUpdate 在 TickScripts 后按配置的顺序表查找对应 System 名并执行 OnUpdate。未在顺序表中列出的 System 最后执行。System 基类添加 `GetName()` 虚方法返回系统名称用于匹配
12. 重构 Application 构造函数：通过 ClassFactory 顺序创建 Engine → RenderContext → GraphicContext。移除 Application 中直接的 Window 创建（`Window::Create()`）、Renderer 初始化（`Renderer::Init()`）、FileSystem 注册。改为 `Engine::Init()` 中注册虚拟路径。保持 ImGuiLayer 创建和 LayerStack 不变。`Application::Run()` 中 Renderer::Render() 替换为 RenderContext::Execute()
13. 更新 Sandbox ExampleLayer2 适配新 API：SandboxApp.cpp 中的 FileSystem 注册移到 Engine 初始化或移除（由 Engine 处理）；Scene 创建改为 Engine::CreateEmptyScene()；所有静态访问改为通过 ClassFactory/Engine 获取。Sandbox2D.h 和 ExampleLayer.h 从 CMakeLists.txt 中移除编译（或 #if 0 排除），仅保 ExampleLayer2 编译
14. 标准化 Layer::OnUpdate 流程 + OnEvent 模式：Application::Run() 主循环顺序确认为 Layer::OnUpdate → ImGui → RenderContext::Execute() → GraphicContext::GetWindow().OnUpdate()。Layer::OnUpdate 内部规范为 用户 Tick → Scene::OnUpdate（System 按配置顺序）。Sandbox ExampleLayer2 添加按键事件示例：OnEvent 捕获空格键 → 修改 SpriteRendererComponent::Color → 不改渲染状态，下一帧 Scene::OnUpdate → RenderSystem 读取 Component → 提交 DrawQuadCommand
