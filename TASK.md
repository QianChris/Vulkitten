1. 修复 IRenderer::Get() 空指针崩溃：将 `s_Current = this;` 移到 Renderer::Init() 和 VkRenderer::Init() 的最开始（在任何 Pass 注册或资源创建之前），确保 SpriteRenderPass 构造函数中 VertexBuffer::Create()→IRenderer::Get().GetResourceManager() 路径可用
2. 清理 Renderer.h 的平台特定依赖：将 `Scope<OpenGLDevice>` 改为 `Scope<IDevice>`，`Scope<GpuResourceManager>` 改为 `Scope<IGpuResourceManager>`，移除 `GetGpuResourceManager()`、`class OpenGLDevice` 前向声明和 `class GpuResourceManager` 前向声明；OpenGLDevice 的具体创建仍在 Renderer.cpp 的 Init() 中通过 make_unique 完成，头文件只暴露 IRenderer 接口类型
3. 将 ShaderManager 并入 IGpuResourceManager 统一资源管理：在 IGpuResourceManager 中添加 LoadShader(virtualPath)→uint64_t 和 GetShaderData(handle)→ShaderData* 虚方法；GpuResourceManager 和 VkGpuResourceManager 实现之；原 ShaderManager 的 ReadFileToString/CollectIncludeDirs/ResolveIncludes 预处理逻辑移入 GpuResourceManager；移除独立的 ShaderManager 类
4. 切换 Shader::Create 为直接加载 .spv 二进制文件：Shader::Create(filepath) 改为拼接 path.vert.spv + path.frag.spv 路径并读取二进制 SPIR-V；Shader::CreateCompute(filepath) 改为加载 path.comp.spv；OpenGLShader 使用 glCreateShaderProgramv 从 SPIR-V 创建 Program；移除 .shader JSON 解析和运行时 GLSL 编译路径
5. 更新所有 Pass 和引擎代码的 shader 加载路径为 .spv：SpriteRenderPass 的 TextureEntity→engine://shaders/TextureEntity（自动拼 .vert.spv/.frag.spv）、Renderer::Init() 预加载引擎 shader→.spv 路径、ExampleLayer2 的 FlatColor→engine://shaders/FlatColor、GpuParticlePass 的 particle compute shader→engine://computeshaders/ParticleXxx（自动拼 .comp.spv）
6. 更新 compile_shader.bat 为可配置编译脚本：compile_shader.bat 接受参数（--target opengl|vulkan、--include-dirs、--output-dir），compile_shader.py 的 glslangValidator 参数根据 target 选择 --target-env opengl 或 --target-env vulkan1.3；支持 --watch 增量模式；添加 compile_shader_vulkan.bat 快捷脚本
7. 端到端验证：Application::SetBackend(RendererBackend::OpenGL) 确保 OpenGL 后端渲染正常（3 Quad + Resize），所有 shader 从 .spv 正确加载；Vulkan 后端编译通过（白屏已知）；清理 OpenGLShader 中废弃的 GLSL 编译和 #include 预处理代码
