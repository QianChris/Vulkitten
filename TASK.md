1. 创建 RHI/Core/Types.hpp：集中定义所有渲染基础类型 — Extent2D/3D、Offset2D/3D、Rect2D、Viewport、ClearColor/ClearDepthStencil/ClearValue(union)、ShaderStage(bitflag)、IndexType、BufferUsage(bitflag)、TextureUsage(bitflag)、MemoryProperty(bitflag)、FilterMode、MipMode、WrapMode、PipelineStage(bitflag)、AccessFlags(bitflag)、ImageLayout、LoadOp、StoreOp。零 API 依赖，纯 C++17。
2. 创建 RHI/Core/Format.hpp：Format 枚举(R8_UNORM..BC7_RGBA_UNORM、D16_UNORM..D32_FLOAT_S8_UINT) + formatByteSize()/formatComponentCount()/isDepthFormat()/isStencilFormat() 工具函数。替代 Buffer.h 中仅顶点属性的 ShaderDataType 枚举。
3. 创建 RHI/Handle.hpp：统一 Handle<Tag> 模板(uint32_t id + Tag 强类型区分)，Tag 结构体(BufferTag/TextureTag/ShaderTag/PipelineTag/GeometryTag/SamplerTag/RenderPassTag/FramebufferTag)，类型别名(BufferHandle/TextureHandle 等)。保留 generation 计数器防 use-after-free。替代 Renderer/Resources/ResourceHandle.h。
4. 创建 RHI/RHIPipelineDesc.hpp：VertexAttribute(location/Format/offset/bufferSlot/stride)、RasterState(CullMode/FrontFace/PolygonMode)、DepthStencilState、BlendState、PipelineDesc(vertexShader+fragmentShader+vertexLayout+rasterState+depthStencilState+blendStates+pushConstantsSize)、GeometryDesc(vertexBuffers[MAX_VB]+indexBuffer+indexType+vertexCount+indexCount——无顶点格式)、SamplerDesc。
5. 重构 IDevice 接口(Device.h → 扩展)：新增 beginFrame()→FrameContext、endFrame(FrameContext)、createCommandBuffer(FrameContext)→ICommandBuffer、createBuffer(desc,data)→BufferHandle、createTexture(desc,data)→TextureHandle、createShader(stage,bytecode)→ShaderHandle、createPipeline(desc)→PipelineHandle、createGeometry(desc)→GeometryHandle、createSampler(desc)→SamplerHandle、createRenderPass(desc)→RenderPassHandle、createFramebuffer(desc)→FramebufferHandle、onResize(w,h)、waitIdle()。同时创建 BufferDesc/TextureDesc/TextureType/RenderPassDesc/FramebufferDesc/AttachmentDesc/SubpassDesc 等描述符结构体。
6. 更新 RHIPipelineDesc.hpp 添加 TextureSlot 和 BufferSlot 槽位声明：PipelineDesc 新增 TextureSlot{slot, Type::Sampled|Storage, stages} 和 BufferSlot{slot, Type::Uniform|Storage|PushConstant, stages, size} 两个内嵌结构体及对应 vector<TextureSlot>/vector<BufferSlot> 成员。后端在 createPipeline 时读取槽位声明预构建 descriptor layout（Vulkan: VkDescriptorSetLayout，GL: uniform location 预映射），上层在 bindTexture/bindUniformBuffer 时使用的 slot 必须在声明范围内。替代 SpriteRenderPass 中手动 texture slot 管理。
7. 创建 RHI/ICommandBuffer.hpp：begin()/end()、barrier(global+texture)、beginRenderPass(RenderPassHandle,FramebufferHandle,Rect2D,span<ClearValue>)/endRenderPass()、bindPipeline(PipelineHandle)/bindGeometry(GeometryHandle)、bindUniformBuffer(slot,BufferHandle,offset,range)/bindStorageBuffer(slot,BufferHandle,offset,range)/bindTexture(slot,TextureHandle,SamplerHandle)、pushConstants(span,offset)、draw(vertexCount)/drawIndexed(indexCount)/dispatchCompute(gx,gy,gz)、copyBuffer/copyBufferToTexture/copyTextureToBuffer、beginDebugLabel/endDebugLabel/insertDebugMarker。Vulkan 真录制，OpenGL 立即执行+状态缓存。bind 方法使用的 slot 与 PipelineDesc 中声明的 TextureSlot/BufferSlot 对应。
8. 重构 FrameContext 为 IDevice 托管的 transient 实例：简化为 frameIndex+swapchainIndex+void* internal，移除显式 CommandPool/CommandBuffer/Fence/Semaphore void*。FrameContext 由 IDevice::beginFrame() 产出、IDevice::endFrame() 消费、createCommandBuffer() 使用。IRenderer::BeginFrame()/EndFrame() 委托给 IDevice。
9. 创建 RHI/IBuffer.hpp、RHI/ITexture.hpp、RHI/IShader.hpp、RHI/IPipeline.hpp、RHI/IGeometry.hpp、RHI/ISampler.hpp：最小查询接口——IBuffer(getSize/map/unmap/flush)、ITexture(getType/getFormat/getExtent/getMipLevels)、IShader(getStage/getEntryPoint)、IPipeline(isCompute)、IGeometry(getVertexCount/getIndexCount)、ISampler(标记型)。大部分操作通过 ICommandBuffer+Handle 完成。
10. 重构 IRenderer 接口：保持 Init/Shutdown/BeginFrame/Execute/EndFrame/OnWindowResize + GetDevice/GetResourceManager/GetRenderGraph/GetShaderLibrary 子系统访问。BeginFrame/EndFrame 委托给 IDevice::beginFrame/endFrame。RendererConfig 保持最小化。
11. 重构 OpenGLDevice 实现扩展 IDevice：beginFrame()/endFrame() 创建 FrameContext+SwapBuffers、createCommandBuffer()→GLCommandBuffer、createBuffer()→GL buffer+Handle、createTexture()→GL texture+Handle、createShader()→编译 SPIR-V+Handle、createPipeline()→GL program+状态缓存+Handle(读取 PipelineDesc::textureSlots/bufferSlots 预分配 uniform location+texture unit 映射表)、createGeometry()→VBO/IBO+Handle、createSampler()→GL sampler+Handle、createRenderPass()/createFramebuffer()→FBO+Handle、onResize()/waitIdle()。通过 ClassFactory::RegisterInterface 注册。
12. 创建 GLCommandBuffer(OpenGL ICommandBuffer 实现)：begin/end 为空或重置状态、barrier 为空(或 glTextureBarrier)、beginRenderPass→glBindFramebuffer+clear、bindPipeline→glUseProgram+加载该 Pipeline 的 slot 映射表(从 PipelineDesc textureSlots/bufferSlots 预构建的 uniform location→texture unit 映射)、bindGeometry→惰性 VAO 创建(Pipeline vertexLayout+Geometry buffers)、bindUniformBuffer→根据 slot 映射表调用 glBindBufferRange、bindTexture→根据 slot 映射表调用 glActiveTexture+glBindTexture+glBindSampler、pushConstants→glUniform*(或 staging UBO)、draw/drawIndexed→glDrawArrays/glDrawElements、dispatchCompute→glDispatchCompute、copyBuffer→glCopyBufferSubData、debug→glPushDebugGroup/glPopDebugGroup。
13. 重构 OpenGLGpuResourceManager：对接新 Handle<Tag> 系统，存储 Handle→GL 资源映射，保留 TickFrame/Gc 和 ShaderData(SPIR-V 加载)机制。
14. 重构 SpriteRenderPass 和 PreparePass 使用 ICommandBuffer：SpriteRenderPass 移除直接 VertexBuffer::Create/VertexArray::Create/Shader::Create 静态调用，改为通过 IDevice 创建 buffers/geometry/pipeline(PipelineDesc 中声明 textureSlots: u_Textures[32] 对应 slot 0-31)。execute 回调中获取 ICommandBuffer 录制 beginRenderPass→bindPipeline→bindGeometry→pushConstants→bindTexture(slot,tex,sampler)→drawIndexed→endRenderPass。顶点格式定义在 PipelineDesc::vertexLayout。PreparePass 使用 ICommandBuffer::beginRenderPass 带 clear values。删除 EndPass(SwapBuffers 已在 IDevice::endFrame)。
15. 重构 OpenGLRenderer 使用新 IDevice/ICommandBuffer 流程：Init 创建 OpenGLDevice+OpenGLGpuResourceManager+RenderGraph+Passes。BeginFrame 调用 m_Device->beginFrame()→FrameContext。Execute 调用 RenderGraph::Execute()(Pass 内部使用 ICommandBuffer)。EndFrame 调用 m_Device->endFrame(ctx)(SwapBuffers)。OnWindowResize 调用 m_Device->onResize+m_RenderGraph->ResizeAllFramebuffers。移除 m_RendererAPI(OpenGLRendererAPI)——所有底层绘制走 ICommandBuffer。
16. 实现 VulkanDevice 完整资源创建：createBuffer→VkBuffer+VMA→Handle、createTexture→VkImage+VkImageView→Handle、createShader→VkShaderModule(SPIR-V)→Handle、createPipeline→VkPipeline(PSO)→Handle(使用 PipelineDesc::textureSlots/bufferSlots 构建 VkDescriptorSetLayout+PushConstantRange)、createGeometry→记录 VkBuffer 引用→Handle、createSampler→VkSampler→Handle、createRenderPass→VkRenderPass→Handle、createFramebuffer→VkFramebuffer→Handle、beginFrame→wait fence+acquire swapchain image+begin command buffer、endFrame→end command buffer+submit+present、createCommandBuffer→从 pool 分配 VkCommandBuffer、onResize→waitIdle+重创 swapchain。
17. 创建 VkCommandBuffer(Vulkan ICommandBuffer 实现)：barrier→vkCmdPipelineBarrier、beginRenderPass→vkCmdBeginRenderPass、bindPipeline→vkCmdBindPipeline+绑定该 Pipeline 的 descriptor set、bindGeometry→vkCmdBindVertexBuffers+vkCmdBindIndexBuffer、bindUniformBuffer/bindStorageBuffer→通过 push descriptor 或预分配 descriptor set 写入(VkDescriptorBufferInfo 从 slot 映射表获取 binding)、bindTexture→VkDescriptorImageInfo(sampler+imageView)写入 descriptor set、pushConstants→vkCmdPushConstants、draw/drawIndexed→vkCmdDraw/vkCmdDrawIndexed、dispatchCompute→vkCmdDispatch、copyBuffer→vkCmdCopyBuffer、debug→vkCmdBeginDebugUtilsLabelEXT/vkCmdEndDebugUtilsLabelEXT。
18. 实现 VkGpuResourceManager 对接新 Handle 系统：存储 Handle→Vulkan 资源映射(VkBuffer/VkImage 等)，保留 TickFrame/Gc(fence-based deferred deletion)。
19. 重构 VkRenderer 对齐 IRenderer 生命周期：BeginFrame→m_Device->beginFrame()→获取 FrameContext+创建 ICommandBuffer、Execute→RenderGraph::Execute()(Pass 录制到 ICommandBuffer)、EndFrame→m_Device->endFrame(ctx)(submit+present)。完善 VkSwapchain 生命周期(create/destroy on resize)。
20. 清理 Vulkan 旧 stub：删除 VkRenderContext/VkPipeline/VkShader 旧 stub 实现(已由 VkDevice+VkCommandBuffer 替代)，保留/适配 VkFrameContext 用于 multi-frame-in-flight 管理。
21. 重构 Application 为完全后端无关：移除 #ifdef VKT_HAS_VULKAN 和 #ifdef 后端选择守卫，移除 Backend/OpenGL/OpenGLRenderer.h 和 Backend/Vulkan/VkRenderer.h 直接 include，创建 RendererFactory 根据 RendererBackend 枚举返回 Scope<IRenderer>。确保 Application.cpp 中无任何条件编译。
22. 端到端验证双后端 ExampleLayer2：OpenGL 后端确认清屏+3 Quad(Green/Red/Checkerboard)渲染正常+窗口 Resize 正常。Vulkan 后端确认同样 3 Quad 渲染正常+窗口 Resize 正常+RenderDoc 可抓帧。
23. 清理死代码：删除 RendererAPI.h/.cpp(ICommandBuffer 替代)、OpenGLRendererAPI.h/.cpp(GLCommandBuffer 替代)、GraphicsContext.h/.cpp(IDevice 替代)、VertexArray.h/.cpp 和 OpenGLVertexArray.h/.cpp(GLCommandBuffer 惰性 VAO 替代)、Renderer/Resources/ResourceHandle.h(RHI/Handle.hpp 替代)、Renderer/Renderer.h/.cpp 基类(合并入 OpenGLRenderer 或移除)、EndPass.h/.cpp(SwapBuffers 已在 IDevice::endFrame)。更新 Vulkitten.h umbrella header 和 CMakeLists.txt。
